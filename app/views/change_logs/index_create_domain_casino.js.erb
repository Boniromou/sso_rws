$('div#content').css({ opacity: "0.0" }).html("<%= escape_javascript(render :file => "change_logs/index_create_domain_casino", :formats => [ :html ]) %>").delay(50).animate({ opacity: "1.0" }, 300);

$(document).ready(function() {
  $('.table').dataTable({
    "order": [[ 0, "asc"]],
    "pageLength" : 50
  });
});

$("[data-content][data-original-title]").popover({trigger: "hover"});

<%= render :partial => "shared/flash_message", :formats => [ :js ] %>