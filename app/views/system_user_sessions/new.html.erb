<!-- ap_login_panel -->
  <%#= form_for(resource, as: resource_name, url: login_path, html: {:class => "smart-form client-form"}) do |f| %>
    <footer>
       <%= render :partial => "shared/caplock_reminder" %>
       <%#= f.submit(t("general.login"), :class => "btn btn-primary") %>
       <%= link_to t("general.login"), "#{new_path}?app_name=#{@app_name}", remote: true, class: "btn btn-primary" %>
       <%= @error_message if @error_message %>
    </footer>
  <%# end %>

